import { InstallmentPipe } from './installment.pipe';

describe('InstallmentPipe', () => {
  it('create an instance', () => {
    const pipe = new InstallmentPipe();
    expect(pipe).toBeTruthy();
  });
});
